[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn kislevsmart.wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
